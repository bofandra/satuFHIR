import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQueryClient } from '@tanstack/react-query'
import { api } from '../lib/api'
import { Button,Card,Field,Input } from '../components/ui'
export function LoginPage(){const[username,setUsername]=useState('learner'),[password,setPassword]=useState(''),[error,setError]=useState('');const nav=useNavigate(),qc=useQueryClient();return <div className="grid min-h-screen place-items-center p-4"><Card className="w-full max-w-md"><div className="mb-6"><h1 className="text-2xl font-bold">FHIRCare</h1><p className="mt-1 text-sm text-muted-foreground">Learn FHIR with SATUSEHAT Sandbox.</p></div><form className="space-y-4" onSubmit={async e=>{e.preventDefault();setError('');try{await api.login(username,password);await qc.invalidateQueries({queryKey:['me']});nav('/')}catch(err:any){setError(err.message)}}}><Field label="Username"><Input value={username} onChange={e=>setUsername(e.target.value)}/></Field><Field label="Password"><Input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></Field>{error&&<p className="text-sm text-red-600">{error}</p>}<Button className="w-full">Sign in</Button></form><p className="mt-5 text-xs leading-5 text-muted-foreground">Zero-database prototype. Credentials are configured server-side through environment variables.</p></Card></div>}
